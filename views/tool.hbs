<div class="tool-details">
  <picture class="tool-detail-img-container">
    <img
      class="tool-detail-img"
      src="{{tool.picture_url}}"
      alt="{{tool.model}}"
    />
  </picture>
  <div class="tool-titles">
    <h2>{{tool.model}}</h2>
    <h3>{{tool.brand}}</h3>
    <div class="tool-buttons">
      {{#if date.start}}
        <form action="/tools/{{tool._id}}/addToCart" method="post">
          <button class="btn btn-turquoise">Add To Cart</button>
        </form>
      {{else}}
        <form action="/tools/{{tool._id}}/addToCart" method="post">
          <label for="start-date">From</label>
          <input type="date" name="start-date" />
          <label for="end-date">To</label>
          <input type="date" name="end-date" />
          <button class="btn btn-turquoise">Add to cart</button>
        </form>
      {{/if}}
      <a href="{{previousUrl}}"><button
          class="btn btn-turquoise back"
        >Back</button></a>
    </div>
    {{#if errorMessage}}
      <p class="error">{{errorMessage}}</p>
    {{/if}}
    <div class="tool-characteristics">
      <div class="tool-description">
        <p><span>Description: </span>{{tool.description}}</p>
      </div>
      <div class="tool-price">
        <p><span>Price: </span>{{tool.price_per_day}}€/day</p>
      </div>
      <div class="tool-tags">
        <p>
          <span>tags: </span>
          {{#each tool.use_case}}
            {{this.name}}
          {{/each}}
        </p>
      </div>

    </div>

  </div>

</div>